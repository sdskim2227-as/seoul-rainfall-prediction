# 데이터 분석 실무 판단 파이프라인

> `pipe-line.md`가 "WISET 실습에서 스크립트를 어떤 순서로 만들지"를 다루는 문서라면,
> 이 문서는 **실무에서 데이터 분석을 할 때 각 단계에서 멈춰서 무엇을 확인하고 판단해야
> 하는지**를 다룬다. 4개 포트폴리오 프로젝트(서울 강수·지하철·전국 전력·가정 전력)를
> 진행하며 실제로 방향을 바꿨거나 놓칠 뻔했던 지점들을 판단 체크리스트로 정리했다.

## 한눈에 보는 흐름

| 단계 | 핵심 판단 질문 |
|---|---|
| 0. 문제 정의 | 이 예측/분석이 실제로 쓸모 있는가? 표본이 통계적으로 충분한가? |
| 1. 데이터 적재 | 원본이 여러 개면 단위계·인코딩이 다 같은가? |
| 2. 품질 점검 | 컬럼 이름이 아니라 실제 값(n_unique·min/max)으로 확인했는가? |
| 3. 결측·이상치 처리 | 결측이 무작위인가, 패턴이 있는가? 그 패턴에 맞게 처리했는가? |
| 4. 집계 단위 결정 | 이 해상도(시간/일/월)가 데이터의 진짜 강점을 살리는가? |
| 5. 타깃 설계 | 이 타깃이 "그냥 지금 값 유지"보다 의미 있는 문제인가? 미래 정보가 새지 않는가? |
| 6. 기준선 계산 | 평균값과 persistence, 둘 다 계산했는가? |
| 7. 데이터 분할 | 무작위 분할이 시간 누수를 만들 위험이 있는가? |
| 8. 모델링 | 단순한 모델부터 순서대로 비교했는가? |
| 9. 평가 | train/test 격차로 과적합을 확인했는가? 지표 함정은 없는가? |
| 10. 시간 누수 검증 | 무작위 분할 성능이 시간순 분할에서도 재현되는가? |
| 11. 문서화 | 판단 근거와 "왜 방향을 바꿨는지"를 기록했는가? |

이 순서는 되돌아갈 수 있다 — 5번(타깃 설계)에서 문제가 발견되면 0번(문제 정의)으로
돌아가는 식으로, 판단은 선형이 아니라 반복적이다.

---

## 단계별 상세

### 0. 문제 정의 & 목적 정렬

**하는 일**: 무엇을 예측/분석할지, 왜 이 데이터를 쓰는지 정한다.

**왜 필요한가**: 문제 정의가 애매하면 뒷단계에서 아무리 정교하게 모델링해도 "그래서 이게
왜 필요한데?"라는 질문에 답을 못 한다.

**판단 포인트**:
- 이 예측이 맞으면 실제로 누가 무엇을 다르게 할 수 있는가? (활용도)
- 이 데이터를 고른 진짜 이유는 무엇인가? 그 이유가 뒤에서 실제로 쓰이는가?
- **표본 크기를 미리 계산해봤는가?** — "연도별 같은 달"처럼 쪼개는 방식은 그럴듯해
  보여도, 4년치 데이터면 "8월"이 4번밖에 안 나온다. 세부 단위로 쪼갤수록 표본이
  줄어드니, 모델을 학습·검증할 최소 표본(수십~수백 개 이상)이 남는지 먼저 셈해본다.

**추가 옵션(상황에 따라)**:
- 미래값을 예측하는 문제라면 → "입력에는 예측 시점 이후 값이 절대 섞이지 않는다"는
  제약을 이 시점에 문장으로 못박아둔다.
- 이해관계자(상사·고객·팀원)가 있다면 → 핵심 질문·성공 기준을 그들과 먼저 합의한다.

---

### 1. 데이터 적재

**하는 일**: 원본을 읽어 구조·기간·행 수·인코딩을 확인한다.

**왜 필요한가**: 여기서 놓친 문제(단위 불일치, 인코딩 깨짐)는 뒤에서 훨씬 비싸게
드러난다 — 이미 만든 파생 변수·모델을 전부 다시 만들어야 할 수 있다.

**판단 포인트**:
- 인코딩이 맞는가? (한글 데이터는 CP949/EUC-KR/UTF-8이 섞여 있는 경우가 흔하다)
- 여러 원본 파일을 합칠 때, 파일마다 단위계(온도 ℃/℉, 강수량 mm/inch 등)가 같은가?
  — 컬럼의 중앙값·범위를 연도/파일별로 비교해서 확인한다.

**추가 옵션**:
- 원본이 커서(수백 MB 이상) 매번 전체를 다시 읽기 부담스럽다면 → 전처리 결과를
  압축 파일(parquet, csv.gz)로 캐싱해 이후 단계가 재사용하게 한다.
- 다중 파일 병합이라면 → 병합 전 경계(파일이 겹치는 날짜 등) 중복을 확인한다.

---

### 2. 데이터 품질 점검

**하는 일**: 결측치·중복행·이상 범위값을 확인하고, 컬럼별 설명·타입·관측범위를 정리한다.

**왜 필요한가**: 컬럼 이름만 보고 값의 성격을 추측하면 틀린다 — 이름은 "확률"인데 실제
값은 0/100 두 개뿐인 플래그였던 사례처럼, 실제 값을 보지 않으면 못 잡는 함정이 많다.

**판단 포인트**:
- 컬럼별로 `n_unique`·`min`/`max`를 직접 찍어봤는가? (이름만 보고 넘어가지 않았는가)
- 이상값이 나오면, 그게 진짜 이상치인지(측정 오류) 실제 사건(뉴스·기록)과 맞아떨어지는
  정상값인지 구분했는가?
- 이 컬럼이 "누적량"(합쳐야 의미 있음)인지 "순간값"(평균 내야 의미 있음)인지 구분했는가?
  — 예: 전력(kW, 순간값)과 서브미터링(Wh, 그 순간의 누적 에너지)을 똑같이 평균 내면
  단위가 틀어진다.

**추가 옵션**:
- 도메인 범위 규칙(습도 0~100% 등)만으로 못 잡는 이상값이 있다면 → 사람이 눈으로
  "상식적으로 말이 되나"까지 확인한다.

---

### 3. 결측·이상치 처리 방침 결정

**하는 일**: 결측·이상치를 어떻게 다룰지(제거/보간/대치) 정하고 실행한다.

**왜 필요한가**: 결측이 무작위가 아니라 특정 시기·조건에 몰려 있으면, 단순히 지우거나
평균으로 채우는 게 데이터의 실제 패턴을 왜곡할 수 있다.

**판단 포인트**:
- 결측이 무작위인가, 특정 시기(계절·요일·특정 구간)에 몰려 있는가? — 몰려 있다면 그냥
  지우는 게 그 시기의 학습을 통째로 못 하게 만들 수 있다.
- 보간한다면 한도(예: 몇 시간 이상 공백은 보간하지 않음)를 정했는가? — 너무 긴 공백을
  보간하면 그건 추정이 아니라 상상이다.
- 처리 후 재확인했는가? — 처리 로직을 적용한 뒤 "제외/보간한 지점 바로 옆"에 예상 못한
  추가 문제(더 긴 공백 등)가 생기지 않았는지 다시 찍어본다.

**추가 옵션**:
- 이상치가 의심되면 → 통계 규칙(중앙값의 N배 등)만 쓰지 말고, 실제 원본을 열어 다른
  비교 대상(다른 역·다른 지역 등)에서도 같은 패턴이 보이는지 교차 확인한다. 통계
  규칙만 쓰면 "새벽 시간대처럼 절대량이 적어 배수로만 튀어 보이는" 정상값을 이상치로
  오인할 수 있다.

---

### 4. (조건부) 집계 단위·해상도 결정

**해당되는 경우**: 원본이 세밀한 단위(분·초 등)라 그대로 쓰기엔 노이즈가 크거나,
목적에 맞는 단위를 새로 정해야 할 때.

**하는 일**: 분→시간, 시간→일처럼 더 큰 단위로 뭉친다.

**왜 필요한가**: 해상도는 "얼마나 세밀하게 보는가"가 아니라 **"이 프로젝트가 가진
데이터의 진짜 강점을 실제로 쓰는가"**로 정해야 한다. 세밀할수록 좋아 보이지만, 이
프로젝트만의 강점(예: 다년치 데이터의 계절성)을 못 살리는 해상도를 고르면 그 강점이
낭비된다.

**판단 포인트**:
- 이 해상도에서 예측하는 문제가, 짧은 기간의 데이터만으로도 풀리는 문제는 아닌가?
  (그렇다면 이 데이터셋만의 강점을 못 쓰는 것)
- 세밀한 해상도(시간 단위 등)는 "설명"(언제·무엇이 원인인지)에, 굵은 해상도(일 단위
  등)는 "예측"에 역할을 나눠 맡길 수도 있다 — 반드시 하나로 통일할 필요는 없다.

**추가 옵션**:
- 뭉치는 방식(합/평균)이 컬럼 성격에 맞는가 재확인한다(2단계에서 정리한 누적량/순간값
  구분을 그대로 적용).
- 뭉친 뒤 "그 단위(시간/일)가 완전히 채워졌는가"를 총량 계산에 쓸 거라면 더 엄격한
  기준(예: 24시간이 다 있어야 그 날짜를 인정)을 적용한다 — 부분적으로만 채워진 단위로
  총량을 내면 실제보다 적게 나온다.

---

### 5. (조건부, 미래값 예측 문제) 타깃 설계

**해당되는 경우**: 원본에 그대로 있는 컬럼이 아니라 "다음 시점 값"처럼 새로 만들어야
하는 타깃일 때.

**하는 일**: `shift(-1)` 등으로 미래 시점 값을 타깃으로 만들고, 누수 없이 만들어졌는지
검증한다.

**왜 필요한가**: 이 단계에서 잘못 정하면 뒷단계 전체(기준선·모델·평가)가 다 잘못된 질문에
답하게 된다. 가장 비싸게 되돌아가야 하는 실수가 여기서 난다.

**판단 포인트**:
- **이 타깃이 "관성"(persistence, 그냥 지금 값을 그대로 다음 값으로 찍기)보다 의미
  있는 문제인가?** 관성이 너무 강한 단위(예: 몇 시간 지속되는 활동의 "다음 시간"
  예측)는 실용성도 낮고 모델이 기준선을 못 이길 위험도 크다. 더 긴 호흡(다음 날·다음
  주)으로 바꾸면 관성이 줄고 모델의 기여가 뚜렷해질 수 있다.
- 데이터에 시간 공백이 있는가? 있다면 `shift(-1)`이 진짜 "바로 다음 시점"과 짝짓는지,
  아니면 공백 너머 몇 시간/며칠 뒤 값과 잘못 짝짓는지 **반드시 확인**한다 — "다음 행의
  실제 시각이 현재+1단위와 정확히 같은가"를 검증한 뒤에만 타깃을 인정한다.
- 무작위 몇 개 지점에서 "i번째 행의 타깃 == 실제 i+1번째 행의 원래 값"을 직접 대조했는가?

**추가 옵션**:
- 표본이 부족해서 타깃을 더 잘게(연도별 등) 못 쪼갠다면 → 타깃은 그대로 두고, 대신
  놓치고 싶지 않은 정보(계절·요일 등)를 **입력 변수**로 넣는다. 타깃을 안 바꿔도 같은
  효과를 표본 손실 없이 얻을 수 있다.

---

### 6. 기준선(baseline) 계산

**하는 일**: 아무 모델도 학습하지 않고, 평균값(회귀)/최빈값(분류)으로만 찍었을 때와
persistence(오늘 값을 그대로 다음 값으로)로 찍었을 때의 성능을 계산한다.

**왜 필요한가**: 기준선 없이 모델 성능만 보면 "괜찮아 보이는 숫자"가 실제로 의미 있는지
판단할 수 없다. 특히 persistence는 순서가 있는 데이터에서 의외로 강력한 경우가 많다.

**판단 포인트**:
- 둘 다 계산했는가? 하나만 계산하면 어느 쪽이 더 강한 기준선인지 모른 채 넘어가게
  된다.
- persistence가 매우 강하다면(예: R²>0.6) → 모델링의 기대 이익이 낮을 수 있다는 신호로
  미리 받아들인다.
- persistence가 약하거나 오히려 평균값보다 나쁘다면(회귀에서 흔함) → 모델이 기여할
  여지가 크다는 뜻이다.

**추가 옵션**:
- 데이터에 순서·인접 관계(시계열·공간)가 있다면 → 평균/최빈값 기준선에 반드시
  persistence를 더한다. 하나만 고르지 말고 **둘 다** 계산해 비교한다.

---

### 7. 데이터 분할

**하는 일**: train/test로 나눈다.

**왜 필요한가**: 분할 방식이 잘못되면(특히 시계열에서 무작위 분할) 성능이 실제보다
부풀려질 수 있다.

**판단 포인트**:
- 시간 순서가 있는 데이터인가? 그렇다면 우선 무작위 분할로 시작하되, 10단계(시간 누수
  검증)에서 날짜순 분할과 반드시 비교한다는 걸 여기서 미리 정해둔다.
- 기준선도 이 분할로 나온 test에 대해 계산했는가? (나중에 모델과 같은 잣대로 비교하기
  위함)

---

### 8. 모델링

**하는 일**: 서로 다른 계열의 모델(선형/배깅(랜덤포레스트)/부스팅(XGBoost) 등)을 같은
입력·같은 분할로 학습·비교한다.

**왜 필요한가**: 한 모델만 써보면 "이게 최선인지" 판단할 근거가 없다. 계열이 다른
모델을 나란히 비교해야 데이터의 성격(선형적인지, 복잡한 상호작용이 있는지)이 드러난다.

**판단 포인트**:
- 단순한 모델(선형회귀)부터 확인했는가? **표본이 작을 때는 복잡한 모델이 항상 이기지
  않는다** — 오히려 가장 단순한 모델이 이기는 경우가 흔하다.
- 입력 변수에 0단계·5단계에서 정한 "놓치면 안 되는 정보"(계절·요일 등)가 실제로 들어가
  있는가?

**추가 옵션**:
- 기본 설정의 부스팅 모델이 기준선보다도 못하다면(회귀 R²가 마이너스 등) → 바로
  하이퍼파라미터를 만지지 말고, 먼저 train 성능과 나란히 비교해 과적합인지 확인한다
  (9단계).

---

### 9. 평가 & 과적합 점검

**하는 일**: test 성능을 지표별로 확인하고, train 성능과 나란히 비교한다.

**왜 필요한가**: 지표 하나만 보면 함정에 빠진다 — 클래스가 쏠려 있으면 accuracy는
높아도 쓸모없고(F1=0), persistence 기준선은 RMSE만 보면 괜찮아 보여도 R²는 마이너스일
수 있다.

**판단 포인트**:
- 분류라면 accuracy만이 아니라 recall/precision/F1도 같이 보는가? (클래스 불균형 함정)
- 회귀라면 RMSE만이 아니라 R²도 같이 보는가? (기준선이 RMSE만으론 괜찮아 보이는 함정)
- **train 성능과 test 성능을 나란히 비교했는가?** train은 거의 완벽한데 test만 유독
  나쁘면 과적합이다 — 특히 표본이 작을 때 부스팅 계열이 train을 통째로 암기하는
  경우가 흔하다.
- 예측값 자체가 물리적으로 말이 되는가? (예: 음수가 나올 수 없는 값에서 음수가 나오면
  과적합의 눈에 보이는 증거)

**추가 옵션**:
- 원인이 "코드 버그"가 아니라 "튜닝 부족/표본 부족"으로 확인되면 → 튜닝(learning_rate↓,
  max_depth↓, 규제↑)으로 해결할지, 아니면 표본이 근본적으로 작아 단순한 모델로
  결론짓고 넘어갈지 **비용 대비 이익을 판단해서** 정한다. 이미 다른 모델이 기준선을
  분명히 이겼다면, 굳이 모든 모델을 튜닝할 필요는 없다.

---

### 10. (조건부, 시계열 데이터) 시간 누수 검증

**해당되는 경우**: 날짜/시간 순서가 의미 있는 데이터(대부분의 실측 데이터). 순서가
무의미한 데이터(무작위 설문 등)라면 건너뛴다.

**하는 일**: 무작위 분할 대신 날짜순 분할(과거로 학습 → 미래를 예측)로 성능이 얼마나
달라지는지 비교한다.

**왜 필요한가**: 무작위 분할은 "미래 정보가 학습에 살짝 섞이는" 상황을 만들 수 있다 —
test 시점 바로 옆(어제·내일) 데이터가 train에 들어가 있으면, 실제 배포 상황(진짜 미래
예측)보다 성능이 부풀려진다.

**판단 포인트**:
- 날짜순 분할에서 성능이 무작위 분할보다 뚜렷이 떨어지는가? 그렇다면 무작위 분할
  성능은 부풀려진 것 — 날짜순 결과를 정직한 성능으로 인용해야 한다.
- 반대로 비슷하거나 오히려 나아졌다면? → 이 데이터는 애초에 "가까운 날짜를 베낄 여지"
  (자기상관)가 약했다는 뜻일 수 있다. train을 아예 안 쓰는 persistence 기준선도 같은
  방향으로 움직이는지 대조해서 확인한다.

---

### 11. 결과 해석 & 문서화

**하는 일**: 최종 결론을 정리하고, 판단 근거와 과정을 기록한다.

**왜 필요한가**: 숫자만 남기면 "왜 이 방향으로 갔는지"가 사라진다. 나중에(또는 다른
사람이) 같은 데이터를 다시 볼 때, 이미 검토하고 기각한 선택지를 또 검토하는 낭비를
막아준다.

**판단 포인트**:
- 계획을 바꾼 지점이 있다면(타깃 단위 변경 등) 그 이유를 남겼는가?
- 결과가 예상과 다르면(모델이 기대만큼 안 나오면) 그 이유를 얼버무리지 않고 정직하게
  적었는가? — "이 데이터 구성으로는 여기가 한계"라는 결론도 유효한 결론이다.
- 이해관계자가 질문했던 것(왜 이 방식이냐, 의미가 있냐 등)에 대한 답을 기록해뒀는가?
  나중에 같은 질문이 다시 나올 수 있다.

---

## 부록 — 반복해서 걸리는 실수 체크리스트

- [ ] 표본 크기를 먼저 계산하지 않고 세부 단위(연도별 같은 달 등)로 쪼개는 타깃을
      설계했는가
- [ ] 컬럼 이름만 보고 값의 성격(이진/연속/누적량/순간값)을 추측했는가
- [ ] 새 스크립트·타깃을 만들기 전에 기존 결과물을 먼저 확인했는가
- [ ] 원본이 여러 파일이라면 단위계가 같은지 확인했는가
- [ ] 결측·공백 처리 후 "처리한 지점 바로 옆"을 재확인했는가
- [ ] shift로 타깃을 만들 때, 데이터에 시간 공백이 있는데도 무작정 shift만 썼는가
- [ ] 기준선(평균값+persistence)을 둘 다 계산하지 않고 모델부터 돌렸는가
- [ ] train/test 성능을 나란히 비교하지 않고 test 지표 하나만 보고 판단했는가
- [ ] 분류에서 accuracy만, 회귀에서 RMSE만 보고 판단했는가
- [ ] 표본이 작은데 부스팅 계열을 기본 설정 그대로 썼는가
- [ ] 방향을 바꾼 이유를 문서에 남기지 않았는가
