# 심화 분석: 시간 누수 검증 · 임계값 최적화 · 확률 보정 · Tweedie · 결합 모델

[README.md](./README.md)의 핵심 결과(baseline → RandomForest → XGBoost, 3개 모델 비교)를
검증하고 보강하기 위해 추가로 진행한 실험들이다. 결론을 뒤집진 않았지만, 결론에 이르는
과정에서 함정을 하나씩 확인했다는 점에서 남겨둔다.

- [1. 시간 누수 검증](#1-시간-누수-검증) — 무작위 분할이 성능을 부풀렸는가?
- [2. 워킹 포워드 재검증과 임계값 함정](#2-워킹-포워드-재검증과-임계값-함정) — 5-fold로도 결론이 유지되는가?
- [3. 확률 보정](#3-확률-보정) — 분류 확률을 그대로 곱해도 되는가?
- [4. Tweedie 회귀](#4-tweedie-회귀) — 0이 많은 타깃 전용 모델을 쓰면 천장을 넘는가?
- [5. 기대 강수량 결합 (분류×회귀)](#5-기대-강수량-결합-분류회귀) — 확률과 조건부 회귀를 곱하면 나아지는가?

---

## 1. 시간 누수 검증

`scripts/22_time_leakage_evaluation.py`로, 지금까지 쓴 무작위 8:2 분할이 (강좌의 다른
프로젝트인 washington 자전거 대여량처럼) 성능을 부풀렸는지 확인했다.

### 방법

두 가지 분할을 비교했다.
- **무작위 분할** (README/REPORT 기본): train 8,765 / test 2,192
- **날짜순 분할** (검증용): 1994-01-01~2017-12-30(train 8,765) → 2017-12-31~2023-12-31(test 2,192)

각 분할에서 학습 없이 계산되는 기준선 두 개와, 튜닝된 모델(19단계 `best_params` 재사용,
재탐색 없음)을 같이 비교했다.

| 분류(F1) | 무작위 분할 | 날짜순 분할 |
|---|---|---|
| neighbor-copy(train에서 가장 가까운 날짜 베끼기) | 0.553 | 0.584 \* |
| persistence(오늘=내일) | 0.570 | 0.643 |
| 랜덤포레스트(튜닝) | 0.573 | 0.608 |
| XGBoost(튜닝) | 0.593 | 0.632 |

| 회귀(R²) | 무작위 분할 | 날짜순 분할 |
|---|---|---|
| neighbor-copy(train) | -0.520 | -0.050 \* |
| persistence(오늘=내일) | -0.530 | -0.524 |
| 랜덤포레스트(튜닝) | 0.146 | 0.143 |
| XGBoost(튜닝) | 0.143 | 0.150 |

\* **`neighbor-copy(train)`의 날짜순 분할 수치는 왜곡돼 있어 그대로 믿으면 안 된다.** 날짜순으로
나누면 test는 전부 train보다 미래라서 "가장 가까운 train 날짜"가 test 2,192일 전체에 대해
train의 마지막 하루로 고정된다(평균 간격 1,096일=3년). 그 하루가 우연히 "비 옴"이었어서
recall=1.0으로 f1이 그럴듯하게 나온 것뿐이다. `persistence`가 분할과 무관하게 항상 정직하게
계산되는 진짜 비교 기준이다.

### 왜 반대 결과가 나왔나

모델 성능만 오른 게 아니라, train을 전혀 쓰지 않는 `persistence` 기준선도 똑같이 올랐다(f1
0.570→0.643, 분류). 모델이 시간 누수로 부풀려졌다면 학습을 안 하는 persistence는 그대로여야
하는데 같이 올랐다는 건, **분할 방식이 아니라 테스트 기간 자체(2018~2023년)가 우연히 더
예측하기 쉬웠다**는 뜻이다.

washington과의 근본적 차이는 **타깃의 날짜 간 자기상관**이다. 자전거 대여량은 오늘과 내일이
비슷해서 무작위 분할이 답을 베끼게 해주지만(neighbor-copy R² 0.717), 강수는 왔다 안 왔다
들쭉날쭉해서(neighbor-copy f1=0.553으로 이미 약함) 애초에 "베낄 답"이 많지 않았다.

### 결론

무작위 분할 성능(F1 0.593, R² 0.143~0.146)은 시간 누수로 부풀려진 게 아니라 **정직한
성능으로 인용해도 된다.** 날짜순 분할이 소폭 더 좋게 나온 것도 "모델이 미래를 더 잘
맞힌다"가 아니라 테스트 기간이 우연히 쉬웠다는 뜻이다.

---

## 2. 워킹 포워드 재검증과 임계값 함정

"시간 누수 검증이 split 1회뿐이라 특정 연도 효과일 수 있다"는 지적을 받아, 확장 윈도우로
5번 반복했다(`scripts/26_rolling_window_evaluation.py`). 각 fold는 이전 모든 과거를
train으로, 다음 약 3년을 test로 쓴다(test 구간이 2009→2023년까지 순차 이동). 재탐색은
하지 않고 19단계 하이퍼파라미터를 그대로 재사용한다.

| 분류(F1, 임계값 0.5 고정) | fold 평균 ± 표준편차 |
|---|---:|
| **persistence(오늘=내일)** | **0.584 ± 0.057** |
| 랜덤포레스트(튜닝) | 0.565 ± 0.058 |
| XGBoost(튜닝) | 0.571 ± 0.057 |

| 회귀(R²) | fold 평균 ± 표준편차 |
|---|---:|
| persistence(오늘=내일) | -0.460 ± 0.144 |
| 랜덤포레스트(튜닝) | **0.169 ± 0.082** |
| XGBoost(튜닝) | 0.159 ± 0.090 |

**회귀는 결론이 그대로다** — 모델이 persistence를 fold 전체에서 안정적으로 이긴다.

**분류는 처음엔 뒤집힌 것처럼 보였다.** 임계값 0.5 고정으로는 persistence(0.584)가
XGBoost(0.571)보다 오히려 높다. 다만 차이(0.013)가 fold 간 표준편차(0.057)보다 작아서 원인을
더 팠다: **train 안에서만(test는 안 보고) F1이 최대가 되는 임계값을 fold마다 새로 골라보니,
5개 fold 모두 0.25~0.35 구간에서 최적값이 나왔다.**

| 분류(F1, train-내부 임계값 최적화) | fold 평균 ± 표준편차 | 고른 임계값 범위 |
|---|---:|---|
| persistence(임계값 개념 없음) | 0.584 ± 0.057 | — |
| 랜덤포레스트(튜닝) | 0.629 ± 0.046 | 0.25~0.30 |
| **XGBoost(튜닝)** | **0.637 ± 0.031** | 0.25~0.35 |

![워킹 포워드 분류](outputs/model/rolling_window_classification.png)
![워킹 포워드 회귀](outputs/model/rolling_window_regression.png)
![워킹 포워드 분류(임계값 최적화)](outputs/model/rolling_window_classification_threshold_opt.png)

그 임계값을 test에 적용하자 XGBoost가 f1=0.637(±0.031)로 persistence(0.584±0.057)를
뚜렷한 차이로, 훨씬 낮은 변동성으로 이겼다. **"persistence가 이긴다"는 처음 결과는 0.5
고정 임계값이 만든 착시였다** — 강수가 다수 클래스(63.7%)가 아니라서 0.5보다 낮은
임계값에서 recall/precision 균형점이 생기기 때문이다.

**최종 결론**: 임계값을 공정하게(train 내부에서만) 고르면 분류 모델이 persistence
기준선을 fold 전반에서 안정적으로 이긴다. 다만 절대 성능(F1 0.64 근방, recall 0.523 문제)이
낮다는 결론은 그대로 유효하다. README/REPORT의 F1=0.593은 이 최적화를 적용하지 않은
0.5 고정 기준 수치이며, 임계값을 최적화하면 이보다 높게 나올 가능성이 크다.

---

## 3. 확률 보정

기대 강수량 결합(5번 섹션)은 분류 확률을 그대로 조건부 회귀값에 곱한다. 트리 모델의
`predict_proba`가 실제 확률처럼 보정돼 있지 않다면 이 곱셈이 왜곡될 수 있어, Brier score와
reliability diagram으로 확인했다(`scripts/24_probability_calibration.py`).

| 모델 | Brier score | 기저 비율만 예측(비교 기준) |
|---|---:|---:|
| XGBoost(튜닝) | **0.1805** | 0.2311 |
| 랜덤포레스트(튜닝) | **0.1801** | 0.2311 |

![확률 보정 곡선](outputs/model/probability_calibration.png)

두 모델 다 "항상 기저 비율(36.3%)만 찍는" 것보다 뚜렷이 나은 Brier score를 냈고, reliability
diagram도 대각선(완벽한 보정)에서 크게 벗어나지 않는다. **별도의 확률 보정(Platt/isotonic)
없이 곱셈을 그대로 써도 되는 수준**으로 판단한다.

---

## 4. Tweedie 회귀

"0이 많고 나머지는 오른쪽으로 긴 연속값" 분포에 직접 대응하는 scikit-learn의
`TweedieRegressor`(power 1.1/1.5/1.9)를 별도로 붙여 비교했다(`scripts/25_tweedie_regression_precip.py`).

| 방식 | RMSE | MedAE | R² |
|---|---:|---:|---:|
| 기대 강수량(결합, 조건부 XGB) | 0.665 | 0.073 | **0.154** |
| 랜덤포레스트(튜닝) | 0.668 | **0.067** | 0.146 |
| XGBoost(튜닝) | 0.669 | 0.080 | 0.143 |
| Tweedie(power=1.9) | 0.684 | 0.090 | 0.105 |
| 선형회귀 | 0.686 | — | 0.100 |
| Tweedie(power=1.5) | 0.688 | 0.104 | 0.094 |
| Tweedie(power=1.1) | 0.693 | 0.131 | 0.083 |

Tweedie는 선형회귀와 비슷하거나 소폭 낮았고, 트리 기반 모델(0.143~0.146)이나 결합(0.154)은
넘지 못했다. **분포 형태에 맞춘 전용 모델을 붙여봐도 R²≈0.15 근방의 천장을 넘지 못했다** —
"이 변수 구성 자체의 정보 한계"라는 결론이 대안 모델 실험으로도 반증되지 않고 오히려
보강됐다.

---

## 5. 기대 강수량 결합 (분류×회귀)

"확률(강수 여부) × 조건부 강수량(비 오는 날만 학습한 회귀)"을 곱한 기대값이, 비 안 오는
날까지 포함해 통째로 학습한 무조건부 회귀보다 나은지 확인했다(`scripts/23_combined_expected_precip.py`).
비 오는 날(`rain_tomorrow`=1)만 3,180행 뽑아 조건부 회귀를 새로 학습하고, 튜닝된
분류기의 확률과 곱했다.

| 방식 | RMSE | MedAE | R² |
|---|---:|---:|---:|
| **기대 강수량(결합, 조건부 XGB)** | **0.665** | 0.073 | **0.154** |
| 랜덤포레스트(튜닝, 무조건부) | 0.668 | **0.067** | 0.146 |
| XGBoost(튜닝, 무조건부) | 0.669 | 0.080 | 0.143 |

**결합 방식이 가장 나은 R²(0.154)를 냈지만, 튜닝된 무조건부 모델(0.143~0.146)과의 차이는
0.01 수준으로 크지 않다.** "결합이 확실히 더 낫다"기보다는 "비슷한 수준에서 소폭 개선"이
정확한 해석이며, 이미 확인한 "이 변수 구성 자체의 정보 천장(R²≈0.14~0.15)"과 일치한다.

**MedAE(중앙값 절대오차)로 보면 순위가 뒤집힌다** — 랜덤포레스트(튜닝, 무조건부)가 0.067로
결합(0.073)보다 낮다(더 좋다). RMSE/R²는 큰 오차(폭우 등 드문 값)에 민감해 결합이 유리하게
나오지만, "전형적인 하루"의 오차만 보면 무조건부 RF가 더 촘촘하다. **결합의 R² 개선은
평범한 날의 정확도가 아니라 드문 큰 값을 상대적으로 덜 틀리는 데서 온다** — "결합이
전반적으로 더 낫다"가 아니라 "결합과 RF(튜닝)는 서로 다른 종류의 오차에서 강점이 갈린다"가
더 정확한 요약이다.

참고로 조건부 회귀 자체만 놓고 보면(테스트의 비 오는 날 795행 한정) R²=0.108,
MedAE=0.278로, "비가 온다는 걸 이미 아는 상태에서 양을 맞히는 것"조차 쉽지 않다.
