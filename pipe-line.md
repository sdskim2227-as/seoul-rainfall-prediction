# 데이터 분석 파이프라인 (v3)

> `01-washington/README.md`의 스크립트 순서(01~15)를 바탕으로 만든 v1 템플릿을,
> `21차시-실습`(서울 날씨 → 다음날 강수 예측)을 진행하며 v2로 한 번 갱신했었다.
> v2는 "범용 단계"와 "이번 프로젝트에서 배운 것"이 단계마다 섞여 있어서, 어떤 데이터에
> 갖다 놔도 그대로 쓰는 부분과 데이터 특성에 따라서만 필요한 부분이 잘 구분되지 않았다.
> v3는 **모든 프로젝트에서 항상 확인하고 넘어가는 "범용 단계"** 와, **각 단계 안에서 데이터
> 특성(다중 파일 여부·시계열 여부·타깃 종류 등)에 따라 조건부로 추가하는 점검** 두 층으로
> 다시 짰다. 위에서부터 순서대로 훑으면서 "항상 할 일"은 그대로 복붙하고, "데이터 특성에
> 따라 추가로 점검"은 조건에 걸릴 때만 끼워 넣는다. `[대괄호]`는 프로젝트에 맞게 채운다.

## 사용법 — 스크립트 번호

프롬프트마다 번호를 고정하지 않고 `NN_`으로 표기했다. 프로젝트마다 앞단 스크립트 개수가
달라서(예: 다중 원본 병합이 끼면 한 칸씩 밀림) 고정 번호를 그대로 쓰면 충돌한다.
**→ 새 스크립트를 만들기 전에 항상 `scripts/` 폴더를 먼저 확인해서 다음 빈 번호로 잡는다.**

---

## 0단계 — 프로젝트 셋업 (범용)

**항상 할 일**
1. `[프로젝트명]/` 폴더를 강사님 스타일(`README.md`/`REPORT.md`/`CLAUDE.md`/
   `requirements.txt`/`data/`/`scripts/`/`outputs/`(quality·eda·model)/`docs/`/`worklogs/`)로
   만들고, `CLAUDE.md`에 작업 규칙(원본 데이터 수정 금지, 모든 수치는 스크립트 출력이어야
   함, 로그 형식)을 먼저 정리해줘.

## 1단계 — 문제 정의 (범용)

**항상 할 일**
- 이 데이터를 고른 이유
- 오늘 풀 문제
- 오늘 데이터에서 확인해볼 것 3가지

**데이터 특성에 따라 추가로 점검**
- 미래값(다음날/다음달 등)을 예측하는 문제라면 → "입력에는 예측 시점 이후 값이 절대 섞이지
  않는다"는 제약을 이 시점에 문장으로 못박아둔다. 나중에 타깃을 만들 때(2.5단계) 그대로
  검증 기준이 된다.

## 2단계 — 워크플로우 문서 (범용)

**항상 할 일**
- 기존/이전 프로젝트의 `CLAUDE_CODE_WORKFLOW.md`를 복사해서, 지금 프로젝트에 맞춰 다시
  써달라고 요청한다.

**데이터 특성에 따라 추가로 점검**
- 참고하는 기존 문서가 분석이 끝난 뒤 쓴 **회고형**이고, 지금 내 프로젝트는 아직 시작
  단계라면 → 그대로 베끼지 말고 "회고 문서가 아니라 계획 문서로 지금 쓴다"고 먼저 합의한다.
  확정된 것(문제 정의, 데이터 개요)과 아직 안 정해진 것을 표로 나눠 쓰고, 마무리 단계에서
  실제 회고로 다시 정리한다. (안 그러면 아직 하지 않은 분석 결과를 지어내게 된다.)

## 3단계 — 데이터 적재 (범용)

**항상 할 일**
1. `data/[파일명].csv`를 읽어서 컬럼 구조·행 수·타입을 확인하는 `NN_load_data.py`를
   작성해줘.

**데이터 특성에 따라 추가로 점검**
- 원본이 여러 파일(연도별 등)로 나뉘어 있다면 → 이 단계보다 먼저 병합 스크립트를 만든다.
  병합 프롬프트에 다음 문장을 추가한다: *"여러 원본 파일을 합칠 때, 파일마다 단위계(온도
  °C/°F, 강수량 mm/inch 등)가 같은지 자료 특성(예: 기온 중앙값)으로 자동 감지해서 다르면
  통일해줘."* — 21차시에서 15개 원본 중 최근 2개년 파일만 미터법으로 수집돼 있었는데,
  그냥 이어붙였으면 최근 데이터가 전부 틀린 채로 분석될 뻔했다.

## 4단계 — 데이터 품질 체크 (범용, 꼭 필요)

**항상 할 일**
1. 결측치·중복행·이상 범위값·데이터 사전(컬럼별 설명·타입·범위)을 뽑아 `outputs/quality/`에
   csv로 저장하는 `NN_data_quality_check.py`를 작성해줘.

**데이터 특성에 따라 추가로 점검**
- 컬럼 이름만 보고 값의 성격(이진/연속/퍼센트 등)을 추측하지 말고 `n_unique`·`min/max`를
  직접 찍어서 확인한다. (예: `precipprob`는 이름은 "확률"인데 실제로는 0/100 두 값만 쓰이는
  플래그였음)
- 도메인 범위 규칙(습도 0~100% 등)만으로는 못 잡는 이상값이 있다 — `data_dictionary.csv`의
  `observed_min`/`observed_max`를 사람이 눈으로 훑어 "상식적으로 말이 되나"까지 확인한다.
  이상값이 나오면 실제 사건(뉴스·기록 등)과 대조해서 진짜 이상치인지 데이터 오류(단위·입력
  실수 등)인지 구분한다.

## 5단계 — 타깃 설계 (조건부: 미래값·파생 타깃을 예측하는 문제일 때만)

> 예측 대상이 원본에 그대로 있는 컬럼이 아니라 "다음 기간 값"처럼 새로 만들어야 하는
> 파생 컬럼일 때만 해당한다. 워싱턴 프로젝트처럼 "같은 날의 다른 값들로 같은 날 타깃을
> 맞히는" 문제라면 이 단계는 건너뛴다.

**해당되면 반드시 할 일**
1. `[타깃 컬럼]`을 `shift(-1)`(또는 필요한 기간만큼)로 끌어와 "다음 [기간] [타깃]" 컬럼을
   만드는 `NN_build_next_[기간]_target.py`를 작성해줘. 입력 피처 쪽은 어떤 컬럼도 앞당기지
   말고, i번째 행의 타깃이 실제 i+1번째 행의 원래 값과 정확히 같은지 무작위 몇 지점을 직접
   대조해서 누수 없이 만들어졌는지 검증해줘. 다음 기간 정보가 없어 타깃이 결측인 행(마지막
   행 등)은 제거해줘.

**데이터 특성에 따라 추가로 점검**
- 시간 누수 검증(12단계)을 모델 다 만든 뒤로 미루지 말고, 타깃을 만드는 이 시점에 먼저
  검증한다 — 훨씬 싸게 문제를 잡을 수 있다. 12단계는 그래도 필요하지만 역할이 다르다
  ("타깃 자체가 새는지"는 여기서, "모델이 미래 정보로 부풀려지는지"는 12단계에서).

## 6단계 — EDA: 범용 3종 + 프로젝트 고유 질문

**항상 할 일**
1. 타겟 변수와 가장 관련 있어 보이는 연속형 변수의 산점도를 그리는 `NN_..._scatter.py`를
   작성해줘.
2. 범주형 변수별 타겟 분포를 비교하는 boxplot을 그리는 `NN_..._boxplot.py`를 작성해줘.
3. 두 범주형 변수 조합(예: 계절×요일)의 평균 타겟값 heatmap을 그리는 `NN_..._heatmap.py`를
   작성해줘.
4. 1단계에서 적어둔 "오늘 데이터에서 확인해볼 것 3가지"를 각각 EDA 스크립트로 만들어줘.
   (위 1~3번과 겹치면 굳이 나누지 말고 합쳐도 된다.)

**데이터 특성에 따라 추가로 점검**
- 타깃이 분류(0/1)라면 → 클래스 균형 체크를 추가한다: `[타깃]==1`과 `==0`의 비율을
  막대그래프로 보여주는 `NN_[타깃]_ratio_eda.py`를 작성해줘. 한쪽으로 심하게 쏠려 있으면
  (예: 90/10) 이후 모델 평가에서 accuracy만 보면 안 되고 recall/precision/F1도 같이 봐야
  한다는 근거가 된다.
- 타깃이 여러 개(예: 분류+회귀+결합)라면 → 산점도·상관관계 분석을 각 타깃 기준으로 한 번씩
  돌려서 비교한다. (21차시에서는 분류 타깃이 회귀 타깃보다 입력 변수와의 상관관계가
  대체로 커서, "분류가 회귀보다 신호가 뚜렷하다"는 판단 근거가 됐다.)

## 7단계 — 기준선·데이터 분할 (범용)

**항상 할 일**
1. **아무 생각 없이 전체 평균(회귀)/최빈값(분류)으로만 찍었을 때의 오차를 계산한다 — 이게
   진짜 기준선이다.** 이 계산은 선택이 아니라 항상 한다. "아무 모델도 학습하지 않고, train의
   평균값(회귀) 또는 최빈 클래스(분류)로만 test 전체를 예측했을 때 RMSE/MAE/R²(회귀) 또는
   accuracy/precision/recall/F1(분류)이 얼마인지 계산하는 `NN_baseline.py`를 작성해줘. 이후
   8단계 실제 모델은 전부 이 기준선보다 확실히 나아야 한다."
2. `train_test_split`으로 train/test를 나누는 `NN_train_test_split.py`를 작성해줘. 1번의
   기준선도 이 분할로 나온 test에 대해 계산한다(나중에 실제 모델과 같은 잣대로 비교하기
   위함).

**데이터 특성에 따라 추가로 점검**
- 시간 순서가 있는 데이터라면 → 우선 무작위 분할로 시작하되, 12단계에서 날짜순 분할과 반드시
  비교한다는 걸 여기서 미리 결정해둔다.
- 데이터에 순서·인접 관계(시계열, 공간 등)가 있다면 → 1번의 "평균/최빈값" 기준선에 더해
  persistence baseline(바로 직전 값을 그대로 예측으로 씀)도 같이 계산한다. 둘 중 하나만
  고르는 게 아니라 **둘 다** 계산해서 어느 쪽이 더 강한 기준선인지 비교한다 — 21차시에서는
  분류는 persistence가 더 강했지만(정확도 68.7% vs 다수클래스 63.7%), 회귀는 반대로 평균값
  기준선(R²≈0)이 persistence(R²=-0.53)보다 나았다. 순서·인접 관계가 없는 데이터라면
  1번(평균/최빈값)만으로 충분하다.

## 8단계 — 베이스라인 모델 3종 (범용 구조, 알고리즘은 조건부)

**항상 할 일**
- 서로 다른 계열의 모델 3종(선형 계열 / 트리 계열 / 부스팅 계열)을 같은 입력·같은 분할로
  학습·비교한다.

**데이터 특성에 따라 추가로 점검**
- 타깃이 연속형(회귀)이라면:
  1. 선형회귀 `NN_linear_regression_[target].py`
  2. 랜덤포레스트 회귀 `NN_random_forest_regression_[target].py`
  3. XGBoost 회귀 `NN_xgboost_regression_[target].py`
  → 평가지표 RMSE/MAE/R²
- 타깃이 범주형(분류)이라면:
  1. 로지스틱회귀 `NN_logistic_regression_[target].py`
  2. 랜덤포레스트 분류기 `NN_random_forest_classifier_[target].py`
  3. XGBoost 분류기 `NN_xgboost_classifier_[target].py`
  → 평가지표 accuracy/precision/recall/F1
- 타깃이 여러 개(분류+회귀+결합 등)라면 → 이 단계 전체를 타깃마다 한 번씩 반복한다.

**참고 — 모델이 기준선보다 못하면 가장 먼저 확인할 것 (쉬운 비유)**

> 21차시에서 기본 설정 그대로 쓴 XGBoost 회귀가 7단계 평균값 기준선보다도 못한(R²가
> 마이너스) 일이 있었다. 원인을 찾을 때 이렇게 접근했다 — 다음에 비슷한 일이 생기면 같은
> 순서로 확인한다.

시험 공부에 비유하면 이해가 쉽다.

- **선형회귀**: 교과서 전체를 대충 훑고 시험 봄 → 모의고사(train)도 어중간, 실전(test)도
  비슷하게 어중간. 안정적이지만 딱히 잘하지도 못함.
- **랜덤포레스트(배깅)**: 친구 여러 명이 각자 따로 공부하고 답을 평균 냄 → 한 명이 헛짚어도
  나머지가 보정해줌. 모의고사는 거의 암기 수준으로 잘 봐도, 실전은 친구들 평균 덕분에
  그럭저럭 완충됨.
- **XGBoost(부스팅)**: 한 명이 오답노트를 계속 고쳐가며 문제 하나하나의 정답을 통째로
  외워버림 → 모의고사(train)는 거의 만점인데, 실전(test)에 나가서는 오히려 아무것도 안
  배운 것보다 못한 점수가 나올 수 있다. 문제집(train 데이터)에 정답 패턴이 아니라 "그날만
  우연히 그랬던" 잡음(노이즈)까지 껴 있는데, 그 잡음까지 몽땅 외워버렸기 때문이다.

**확인 절차**:
1. 문제의 모델을 train 데이터로도 다시 평가해서, train 성능과 test 성능을 나란히 비교한다.
   **train은 거의 완벽한데 test만 유독 나쁘면 과적합이다**(21차시 XGBoost: train
   R²=0.996, test R²=-0.02).
2. 예측값 자체가 물리적으로 말이 안 되는지 확인한다(21차시에서는 강수량인데 예측값의
   23%가 음수로 나왔다 — "비가 마이너스로 온다"는 신호로 과적합을 눈으로 확인).
3. 같은 계열이라도 배깅(랜덤포레스트)과 부스팅(XGBoost)은 과적합 양상이 다르다는 걸
   기억한다 — 배깅은 여러 모델의 평균이라 어느 정도 자동으로 완충되지만, 부스팅은 규제를
   직접 걸어주지 않으면 잡음까지 외운다.
4. 원인이 "코드 버그"가 아니라 "튜닝 부족"이라는 게 확인되면, 10단계(하이퍼파라미터 튜닝)에서
   `learning_rate`를 낮추고 `max_depth`를 줄이고 `early_stopping_rounds`나
   `subsample`/`colsample_bytree` 같은 규제를 넣어 해결한다.

## 9단계 — 모델 비교 (범용)

**항상 할 일**
1. 8단계 모델들의 지표를 7단계 기준선과 함께 한 표·차트로 비교하는
   `NN_model_comparison_chart.py`를 작성해줘.

## 10단계 — 튜닝 (범용)

**항상 할 일**
1. 랜덤포레스트/XGBoost에 GridSearchCV로 하이퍼파라미터 튜닝을 적용하는
   `NN_hyperparameter_tuning.py`를 작성해줘.

## 11단계 — 변수중요도 (범용)

**항상 할 일**
1. 튜닝된 모델의 변수중요도를 차트로 그리는 `NN_..._feature_importance_chart.py`를
   작성해줘.

## 12단계 — 시간 누수 검증 (조건부: 시계열 데이터일 때 필수)

> 날짜/시간 순서가 의미 있는 데이터(대부분의 실측 데이터)라면 필수. 순서가 무의미한
> 데이터(예: 무작위 설문 응답)라면 건너뛴다.

**해당되면 반드시 할 일**
1. random split 대신 날짜순 분할(가능하면 "이전 해 학습→다음 해 예측")했을 때 성능이 얼마나
   떨어지는지 비교하는 `NN_time_leakage_evaluation.py`를 작성해줘. "학습 없이 옆 날짜 값을
   베끼기만 한" 기준선도 같이 계산해줘.

**데이터 특성에 따라 추가로 점검**
- 5단계(타깃 설계)를 거친 미래값 예측 문제라면 → "타깃 자체의 누수"는 이미 5단계에서 검증이
  끝났으므로, 여기서는 "모델이 학습 시점 이후 데이터로 성능을 부풀리고 있지 않은지"에
  집중한다.

## 13단계 — 도메인 특화 효과 검정 (조건부: 의미 있는 이벤트/구간 변수가 있을 때, 선택)

**해당되면 할 일**
1. `[공휴일/이벤트/특수일]` 여부가 타겟에 통계적으로 유의한 차이를 만드는지 t-test로
   검정하는 `NN_[event]_effect_check.py`를 작성해줘.

## 14단계 — 이상치 탐지 (범용)

**항상 할 일**
1. Isolation Forest로 이상치를 골라내고, ablation(각 변수를 평균으로 되돌렸을 때 이상
   점수가 얼마나 회복되는지)으로 원인을 역추적하는 `NN_anomaly_detection.py`를 작성해줘.

## 15단계 — 예측 잔차 분석 (범용)

**항상 할 일**
1. 최종 튜닝 모델로 전체 데이터를 다시 예측해서, 가장 크게 틀린 날/케이스와 그 원인, 그리고
   14단계 이상치 목록과의 겹침을 분석하는 `NN_predict_residuals.py`를 작성해줘.

## 마무리 — 문서화 (범용)

**항상 할 일**
1. 산출물을 바탕으로 `README.md`(모델 비교표, 핵심 발견, 그림, 폴더 구조, 재현 방법)와
   `REPORT.md`(상세 분석)를 작성해줘.
2. 어떤 판단을 사람이 내리고 어떤 걸 Claude Code가 맡았는지 `docs/CLAUDE_CODE_WORKFLOW.md`를
   (2단계에서 계획 문서로 썼다면) 실제 회고 내용으로 다시 정리해줘.

---

## 부록 — 21차시(서울 날씨 → 다음날 강수 예측) 실제 스크립트 번호

템플릿 단계와 실제 번호가 프로젝트 사정에 따라 어떻게 밀리는지 보여주는 예시.

| 번호 | 파일명 | 해당 단계 |
|---|---|---|
| 01 | `merge_seoul_weather.py` | 3단계 조건부(다중 원본 병합) |
| 02 | `load_data.py` | 3단계 |
| 03 | `data_quality_check.py` | 4단계 |
| 04 | `build_next_day_target.py` | 5단계(조건부, 미래값 예측) |
| 05 | `precip_scatter.py` | 6단계 |
| 06 | `rain_ratio_eda.py` | 6단계 조건부(클래스 균형) |
| 07 | `seasonality_eda.py` | 6단계(문제정의 고유 질문) |
| 08 | `season_precip_boxplot.py` | 6단계 |
| 09 | `season_weekday_rain_heatmap.py` | 6단계 |
| 10 | `feature_correlation_eda.py` | 6단계(문제정의 고유 질문) |
| 11~ | (진행 예정) | 7단계(기준선·분할) → 8단계(분류 3종) → ... |

## 부록 — 자주 걸리는 실수 체크리스트

- [ ] "아무 생각 없이 평균/최빈값으로만 찍었을 때의 오차"를 기준선으로 반드시 계산했는가
      (선택이 아니라 7단계에서 항상 하는 것)
- [ ] 새 스크립트 번호를 정하기 전에 `scripts/` 폴더를 먼저 확인했는가
- [ ] 원본 파일이 여러 개라면 파일별 단위계가 같은지 확인했는가
- [ ] 컬럼 이름만 보고 값의 성격을 추측하지 않고 `n_unique`·`min/max`로 직접 확인했는가
- [ ] 미래값을 예측하는 문제라면, 타깃을 만드는 시점에 바로 누수 검증을 했는가
- [ ] 분류 타깃이라면 클래스 균형을 확인했는가 (쏠려 있으면 accuracy 외 지표도 같이 볼 것)
- [ ] `CLAUDE_CODE_WORKFLOW.md`를 아직 안 한 작업의 결과인 것처럼 회고형으로 미리 쓰지
      않았는가
